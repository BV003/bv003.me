---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Michael Liu's personal blog - AI research, engineering, and more." />
    <link rel="icon" type="image/png" href="/profile.png" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="alternate" type="application/rss+xml" title="RSS Feed" href="/rss.xml" />
    <!-- KaTeX CSS for math rendering -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css" integrity="sha384-n8MVd4RsNIU0tAv4ct0nTaAbDJwPJzDEaqSD1odI+WdtXRGWt2kTvGFasHpSy3SV" crossorigin="anonymous">
    <title>{title}</title>
    <style is:global>
      :root {
        --color-text: #111;
        --color-bg: #fffff8;
        --color-border: #c8c5bc;
        --color-accent: #a00000;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      html {
        font-family: Georgia, 'Times New Roman', serif;
        font-size: 10px;
        line-height: 1.4;
        color: var(--color-text);
        background: var(--color-bg);
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }
      
      body {
        min-height: 100vh;
      }
      
      a {
        color: var(--color-accent);
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      h1, h2, h3, h4, h5, h6 {
        font-weight: 400;
        line-height: 1.2;
        letter-spacing: 0;
        margin-top: 1.4rem;
        margin-bottom: 0;
      }
      
      h1 {
        font-size: 2.5rem;
        line-height: 1.184;
        margin-top: 1.568rem;
        margin-bottom: 1.568rem;
      }
      
      h2 {
        font-size: 2.1rem;
        line-height: 0.9333;
        margin-top: 1.8667rem;
      }
      
      h3 {
        font-size: 1.8rem;
        line-height: 1.0889;
        font-style: italic;
        margin-top: 2.1778rem;
      }
      
      h4 {
        font-size: 1.5rem;
        line-height: 1.2;
        margin-top: 1.8rem;
      }
      
      h5 {
        font-size: 1.4rem;
      }
      
      h6 {
        font-size: 1.3rem;
      }
      
      p {
        margin: 1.4rem 0;
        font-size: 1.4rem;
        line-height: 2rem;
        text-align: justify;
      }
      
      ul, ol {
        margin: 1.4rem 0;
        padding-left: 6%;
      }

      ol {
        list-style-type: decimal;
      }
      
      li {
        margin-bottom: 0.5rem;
        font-size: 1.4rem;
        line-height: 2rem;
        text-align: justify;
      }
      
      li > ul, li > ol {
        margin-top: 0.5rem;
        margin-bottom: 0.5rem;
      }
      
      code {
        font-family: Consolas, 'Liberation Mono', Menlo, Courier, monospace;
        font-size: 1rem;
        line-height: 1.42;
        background: transparent;
        color: #111;
        padding: 0;
        border-radius: 4px;
      }
      
      pre {
        background: #fff;
        padding: 0.5rem;
        border: 1.5px solid #999;
        border-radius: 4px;
        overflow-x: auto;
        margin: 1.42rem 0;
        position: relative;
      }
      
      pre code {
        background: none;
        padding: 0;
        color: #111;
        font-size: 1rem;
        line-height: 1.42;
      }

      /* Astro/Shiki adds an inline dark theme to highlighted blocks. */
      pre.astro-code {
        background: #fff !important;
        color: #111 !important;
      }

      pre.astro-code code {
        color: #111 !important;
      }
      
      blockquote {
        border-left: 3px solid var(--color-accent);
        padding: 0.5rem 0 0.5rem 1.25rem;
        margin: 1.8rem 0;
        font-style: italic;
        color: #333;
      }
      
      blockquote p {
        margin-bottom: 0.75rem;
      }
      
      blockquote p:last-child {
        margin-bottom: 0;
      }
      
      hr {
        border: none;
        border-top: 1px solid var(--color-border);
        margin: 3rem 0;
      }
      
      img {
        max-width: 100%;
        height: auto;
      }

      figure {
        margin: 2rem 0;
      }

      figure img {
        display: block;
        border-radius: 4px;
      }

      figcaption {
        text-align: center;
        font-size: 0.875rem;
        color: #888;
        margin-top: 0.5rem;
      }

      /* Table styles */
      table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
        margin: 1.8rem auto;
        font-size: 0.95rem;
        border-top: 2px solid #333;
        border-bottom: 2px solid #333;
      }

      thead {
        border-bottom: 1px solid #333;
      }

      th {
        padding: 0.55rem 0.5rem;
        text-align: left;
        font-weight: 400;
      }

      td {
        padding: 0.55rem 0.5rem;
        border-bottom: 1px solid #ddd8cc;
        vertical-align: top;
        line-height: 1.5;
      }

      tbody tr:nth-child(odd) {
        background: #fbf7f0;
      }

      tbody tr:last-child td {
        border-bottom: none;
      }



      /* KaTeX adjustments for clean look */
      .katex {
        font-size: 1em;
      }
      
      .katex-display {
        margin: 1.5rem 0;
        overflow-x: auto;
        overflow-y: hidden;
      }

      /* Footnote styles */
      .footnote-ref {
        vertical-align: super;
        font-size: 0.75em;
        line-height: 0;
      }
      
      .footnote-ref a {
        text-decoration: none;
        padding: 0 2px;
      }
      
      .footnote-backref {
        text-decoration: none;
        margin-left: 0.5rem;
      }
      
      .footnotes {
        margin-top: 3rem;
        padding-top: 2rem;
        border-top: 1px solid var(--color-border);
        font-size: 0.9em;
      }
      
      .footnotes ol {
        padding-left: 1.5rem;
        list-style-type: decimal;
      }
      
      .footnotes li {
        margin-bottom: 0.75rem;
        list-style-type: decimal;
      }
      
      .footnotes li::marker {
        font-weight: 500;
      }

      @media screen and (min-width: 800px) {
        html { font-size: 11px; }
      }

      @media screen and (min-width: 900px) {
        html { font-size: 12px; }
      }

      @media screen and (min-width: 1000px) {
        html { font-size: 13px; }
      }

      @media screen and (min-width: 1100px) {
        html { font-size: 14px; }
      }
    </style>
  </head>
  <body>
    <slot />
  </body>
</html>
